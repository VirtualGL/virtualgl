* Conventions Used in This Document

This document assumes that VirtualGL will be installed in the default
directory (''/opt/VirtualGL'').  If your installation of VirtualGL resides in a
different directory, then adjust the instructions accordingly.
