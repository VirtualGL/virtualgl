#/bin/bash
CERTFILE=~/rrcert.pem
if [ -n "$1" ]
then
	CERTFILE=$1
fi
openssl req -new -x509 -days 365 -nodes -config /etc/rrcert.cnf -out $CERTFILE -keyout $CERTFILE
openssl x509 -subject -dates -fingerprint -noout -in $CERTFILE
